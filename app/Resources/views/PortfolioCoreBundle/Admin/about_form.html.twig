{{ form_start(form,{'attr': {'class': 'text-left m-auto w-50p@md+'}}) }}
      <div class="form-group">
          {{form_label(form.profil)}}
          {{form_widget(form.profil)}}
          {{form_errors(form.profil)}} 
      </div> 
      <div class="form-group">
          {{form_label(form.description)}}
          {{form_widget(form.description)}}
          {{form_errors(form.description)}} 
      </div>   

      {{form_rest(form)}}
      <div class="form-group">
          <button type="submit" class="form-control pY-10 bdrs-0 btn-primary">Enregistrer</button>
      </div>
  </form> 